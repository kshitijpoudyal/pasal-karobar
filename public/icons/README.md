# PWA icons

# PWA icons

`icon-192.png` and `icon-512.png` — brand marks for the web app manifest (`src/app/manifest.ts`).

Regenerate (requires project `sharp` from Next.js):

```bash
node scripts/generate-pwa-icons.mjs
```

Referenced by `src/app/manifest.ts`.
